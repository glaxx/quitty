[General]
; Please provide the path to the Zuwendungsbestaetigung template.
TemplatePath = dist/usr/share/quitty/zuwendungsbestatigung_geldzuwendung.tex

; 0, PanicLevel: highest level of severity.
; 1, FatalLevel: Errors that severe that this application cannot succeed.
; 2, ErrorLevel: Used for errors that should definitely be noted.
; 3, WarnLevel:  Non-critical entries that deserve eyes.
; 4, InfoLevel:  General operational entries about what's going on inside the
;                application.
; 5, DebugLevel: Usually only enabled when debugging. Very verbose logging.
LogLevel = 5

